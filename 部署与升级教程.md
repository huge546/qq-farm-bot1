# QQ 农场机器人 部署与升级教程

本教程适用于已经包含「多租户卡密体系」的版本，对应仓库：

- 代码仓库：`git@github.com:huge546/qq-farm-bot1.git`
- 分支：`card-system`
- 上游源码：`https://github.com/liyangpengs/qq-farm-bot.git`

默认后台端口为 `3007`，管理员初始账号为 `admin`，初始密码为 `admin`。

## 1. 服务器要求

- 一台 Linux 服务器（本教程以 Debian / Ubuntu 为例）
- Docker 20+
- Docker Compose 2+
- Git

检查环境：

```bash
docker --version
docker compose version
git --version
```

## 2. 获取代码

建议使用 SSH 方式克隆（部分服务器直连 GitHub HTTPS 会被限制）：

```bash
cd /root
git clone git@github.com:huge546/qq-farm-bot1.git qq-farm-bot-master
cd qq-farm-bot-master
git checkout card-system
```

如果服务器已经配置好 SSH key，克隆后可以直接使用；没有配置的话，先执行：

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
```

把输出的公钥添加到 GitHub 的 Settings → SSH and GPG keys。

## 3. 配置环境变量

复制并编辑 `.env`：

```bash
cd /root/qq-farm-bot-master
nano .env
```

内容如下：

```env
PORT=3007
TZ=Asia/Shanghai
NODE_ENV=production
```

- `PORT`：后台对外端口，默认 `3007`
- `TZ`：时区，默认 `Asia/Shanghai`
- `NODE_ENV`：保持 `production`

保存后退出（nano 中按 `Ctrl+O` 回车保存，`Ctrl+X` 退出）。

## 4. 构建并启动

```bash
cd /root/qq-farm-bot-master
docker compose up -d --build
```

第一次构建需要几分钟，完成后查看状态：

```bash
docker compose ps
```

看到 `STATUS` 为 `Up (healthy)` 即表示成功：

```text
NAME          IMAGE                COMMAND                  SERVICE       STATUS
qq-farm-bot   qq-farm-bot:latest   "docker-entrypoint.s…"   qq-farm-bot   Up (healthy)
```

查看日志：

```bash
docker compose logs -f qq-farm-bot
```

退出日志按 `Ctrl+C`。

## 5. 访问后台

启动后访问：

- 本机测试：`http://127.0.0.1:3007`
- 域名访问：`https://你的域名`

首次登录使用：

```text
账号：admin
密码：admin
```

登录后请立即到设置页修改管理员密码。

> 说明：`docker-compose.yml` 默认将端口绑定为 `127.0.0.1:3007`，即只允许本机访问。这样做是为了配合 Nginx 反向代理，避免后台端口直接暴露到公网，更安全。

## 6. 配置 Nginx 反向代理 + HTTPS

假设域名已经解析到服务器 IP，Nginx 配置示例：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:3007;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

修改域名后测试并重载：

```bash
nginx -t
systemctl reload nginx
```

## 7. 数据持久化与备份

Docker 数据保存在两个命名卷里：

- `qq-farm-data`：账号、卡密、配置等核心数据
- `qq-farm-logs`：日志数据

查看卷：

```bash
docker volume ls
```

备份示例：

```bash
mkdir -p /root/backup
docker run --rm -v qq-farm-data:/data -v /root/backup:/backup alpine tar czf /backup/qq-farm-data-$(date +%Y%m%d).tar.gz -C /data .
docker run --rm -v qq-farm-logs:/data -v /root/backup:/backup alpine tar czf /backup/qq-farm-logs-$(date +%Y%m%d).tar.gz -C /data .
```

恢复时，将备份包解压回对应卷即可。

## 8. 日常运维命令

查看状态：

```bash
docker compose ps
```

查看最近日志：

```bash
docker compose logs --tail 100 qq-farm-bot
```

重启服务（保留数据，只重建容器）：

```bash
docker compose up -d --build
```

停止服务：

```bash
docker compose down
```

停止并删除容器和网络（不会删除数据卷）：

```bash
docker compose down
```

彻底清理（同时删除数据卷，慎用）：

```bash
docker compose down -v
```

## 9. 后续升级

### 9.1 日常升级（拉取当前仓库的最新代码）

当 `card-system` 分支有更新时：

```bash
cd /root/qq-farm-bot-master
git pull
docker compose up -d --build
```

构建完成后确认：

```bash
docker compose ps
```

看到 `Up (healthy)` 即升级完成。

### 9.2 上游项目出新版本后，合并卡密系统

上游仓库地址：`https://github.com/liyangpengs/qq-farm-bot.git`

在本地（有完整开发环境的那台机器）操作：

```bash
cd qq-farm-bot-master
git fetch upstream
git checkout card-system
git merge upstream/master
```

如果出现冲突，通常只会出现在以下卡密相关文件中：

- `core/src/controllers/admin/auth-routes.ts`
- `core/src/controllers/admin/middleware.ts`
- `core/src/controllers/admin/account-routes.ts`
- `core/src/models/store/accounts.ts`
- `core/src/models/store/index.ts`
- `core/src/models/card-store.ts`
- `core/src/models/user-store.ts`
- `core/src/runtime/runtime-engine.ts`
- `core/src/types/account.ts`
- `web/src/views/Login.vue`
- `web/src/views/AdminCards.vue`
- `web/src/views/MyAccount.vue`
- `web/src/views/Settings.vue`
- `web/src/components/Sidebar.vue`
- `web/src/router/index.ts`
- `web/src/router/menu.ts`
- `web/src/stores/user.ts`
- `docker-compose.yml`

解决冲突后，务必在本地先验证编译：

```bash
pnpm install
pnpm -C core build:ts
pnpm -C web build
```

编译通过后再提交并推送：

```bash
git add .
git commit -m "merge: 合并上游最新版本并保留卡密体系"
git push origin card-system
```

推送到 GitHub 后，回到服务器执行：

```bash
cd /root/qq-farm-bot-master
git pull
docker compose up -d --build
docker compose ps
```

### 9.3 升级前建议

升级前先备份数据卷（参考第 7 节），避免意外。

## 10. 卡密系统说明

本版本在原有后台基础上新增：

- 卡密管理：管理员可生成、查看、删除卡密
- 用户注册/登录：普通用户通过卡密注册和登录
- 账号配额：普通用户受卡密套餐的账号数量上限约束
- 角色隔离：普通用户只能操作自己的账号

管理员登录后台后，在卡密管理页面生成卡密，分发给用户即可。

## 11. 常见问题

### 11.1 容器状态一直是 unhealthy

查看健康检查失败原因：

```bash
docker inspect --format='{{json .State.Health}}' qq-farm-bot
docker compose logs --tail 50 qq-farm-bot
```

常见原因：

- 代码中文被存成乱码，导致编译失败
- 端口被占用
- 构建过程网络下载依赖失败

### 11.2 域名能访问，但 IP:3007 打不开

这是正常现象。当前端口只绑定 `127.0.0.1`，仅允许本机访问，域名通过 Nginx 转发到本机端口。建议保持此配置，更安全。

### 11.3 如何修改后台端口

编辑 `.env` 中的 `PORT`，然后执行：

```bash
docker compose up -d --build
```

### 11.4 Windows 上编辑文件后中文变乱码

在 Windows 上修改代码后，提交前务必用 UTF-8 编码保存，并在本地执行：

```bash
pnpm -C core build:ts
```

确认编译通过后再提交，避免把乱码推到服务器导致构建失败。