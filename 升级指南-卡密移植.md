# 卡密系统移植指南（给下一次升级用）

本仓库 = liyangpengs/qq-farm-bot 上游 master + 多租户卡密体系（移植自 8.25 旧版）。
所有卡密改动都在 `card-system` 分支的单个提交里，基于 `upstream/master`。

## 涉及文件（24 个）

新增 6 个：
- core/src/models/card-store.ts
- core/src/models/user-store.ts
- web/src/utils/clipboard.ts
- web/src/views/AdminCards.vue
- web/src/views/MyAccount.vue
- .gitattributes（换行符规则 * text=auto，保证以后合并无换行噪音）

修改 18 个：
- core/src/controllers/admin/auth-routes.ts（注册/登录/角色会话/卡密接口）
- core/src/controllers/admin/middleware.ts（角色鉴权 + 账号归属守卫）
- core/src/controllers/admin/socket.ts（角色感知的实时推送）
- core/src/controllers/admin/context.ts（会话上下文）
- core/src/controllers/admin/account-routes.ts（配额/归属校验 + 账号备注必填 + 系统配置变更自动重启）
- core/src/models/store/accounts.ts（userId 字段 + countAccountsByUser）
- core/src/models/store/index.ts（导出 countAccountsByUser）
- core/src/runtime/runtime-engine.ts（时长卡到期自动降级）
- core/src/types/account.ts（Account 增加 userId）
- web/src/views/Login.vue（卡密注册登录）
- web/src/views/Settings.vue（系统设置仅管理员可见，保留 MeoW 推送）
- web/src/components/Sidebar.vue（菜单按角色过滤 + 用户下拉“我的账号”）
- web/src/router/index.ts / menu.ts（角色路由）
- web/src/stores/user.ts（角色状态）
- docker-compose.yml / core/Dockerfile（健康检查用 127.0.0.1 + 端口仅本机绑定）
- README.md（文档）

## 上游出新版本后怎么把卡密再合进去

```bash
git fetch upstream
git checkout card-system
git merge upstream/master
```

有冲突时，通常只会在上面 18 个“修改”文件里出现，解决完提交即可：

```bash
git add .
git commit
```

如果想把合并做成“重放我们的提交”，也可以改用 rebase：

```bash
git checkout card-system
git rebase upstream/master
```

## 两个容易丢的运行时修复（移植时千万别漏）

普通用户添加账号时：
1. `core/src/models/store/accounts.ts` 的 `module.exports` 必须导出 `countAccountsByUser`，并且新建账号时要写入 `userId`；
2. `core/src/controllers/admin/account-routes.ts` 的 payload 必须带上 `...(boundUserId ? { userId: boundUserId } : {})`。
漏掉任何一个，普通用户建号会报错或账号不归属到自己名下。

## 验证

```bash
pnpm install
pnpm -C core build:ts        # 后端编译
pnpm -C web build            # 前端构建
```

## 部署

- 源码方式：`bash ./start.sh`
- Docker：`docker compose up -d --build`
- 面板默认 `http://<IP>:3007`，管理员 admin/admin，首次登录后立即改密码

## 重新生成补丁（备用）

```bash
git diff upstream/master --ignore-cr-at-eol > card-system.patch
git apply --ignore-space-at-eol card-system.patch   # 在新版本上应用
```
